import styles from "./Channel.module.css";

import Card from "../components/layout/Card";

const Channel = () => {
  return (
    <section className={styles.container_channels}>
      <Card img="" alt="Globo" name="Globo" />
    </section>
  );
};

export default Channel;
